name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: bun-linux-x64
            artifact: ccrecall-linux-x64
          - os: ubuntu-latest
            target: bun-linux-arm64
            artifact: ccrecall-linux-arm64
          - os: macos-latest
            target: bun-darwin-x64
            artifact: ccrecall-darwin-x64
          - os: macos-latest
            target: bun-darwin-arm64
            artifact: ccrecall-darwin-arm64
          - os: windows-latest
            target: bun-windows-x64
            artifact: ccrecall-windows-x64.exe

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v6

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build
        run:
          bun build --compile --target=${{ matrix.target }}
          src/index.ts --outfile ${{ matrix.artifact }}

      - uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

  release:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v7
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
